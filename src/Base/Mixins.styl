cf()
	&::before
	&::after
		content: ""
		display: block
		clear both

center-pos($top = -50%, $left = -50% )
	position absolute
	top 50%
	left 50%
	transform: translate($top,$left)
flex-center()
	display flex
	justify-content center
	align-items: center

